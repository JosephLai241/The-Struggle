use crate::model::Job;

use ansi_term::{Color::{Blue, Green, Purple, Red, Yellow}, Style};

// Blue == PENDING
// Yellow == IN PROGRESS
// Purple == OFFER RECEIVED
// Green == HIRED
// Red == REJECTED

pub fn paint(line: String) {
    return;
}